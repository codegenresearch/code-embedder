import argparse\n\nfrom loguru import logger\n\nfrom src.code_embedding import CodeEmbedder, ScriptMetadataExtractor, ScriptContentReader\n\n\ndef parse_arguments():\n    parser = argparse.ArgumentParser(description='Process Readme files and embed code snippets.')\n    parser.add_argument(\n        '--readme-paths',\n        nargs='+',\n        type=str,\n        help='Paths to Readme files',\n        default=['README.md']\n    )\n    return parser.parse_args()\n\n\nif __name__ == '__main__':\n    args = parse_arguments()\n    script_metadata_extractor = ScriptMetadataExtractor()\n    script_content_reader = ScriptContentReader()\n    code_embedder = CodeEmbedder(\n        readme_paths=args.readme_paths,\n        script_metadata_extractor=script_metadata_extractor,\n        script_content_reader=script_content_reader,\n    )\n    code_embedder()\n    logger.info('Code Embedder finished successfully.')\n